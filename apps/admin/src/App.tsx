const presets = ['Firm Admin', 'Attorney', 'Paralegal', 'Intake', 'Billing', 'Read-only'];
const tools = ['mock_search_matters', 'mock_get_matter_context', 'mock_create_task'];
export function App() { return <main className="shell"><section className="hero"><p className="eyebrow">Legal MCP Gateway</p><h1>Permission-scoped MCP access for legal teams.</h1><p>This is the admin UI scaffold. Real persistence, auth, OAuth setup, and connector management come next.</p></section><section className="grid"><article className="card"><h2>Users</h2><p>Create users, assign job-title presets, issue MCP access tokens, and preview effective permissions.</p><button type="button">Add user placeholder</button></article><article className="card"><h2>Connectors</h2><p>Install vendor connectors and bring your own OAuth/API credentials. The scaffold ships with Mock Legal.</p><button type="button">Connect vendor placeholder</button></article><article className="card"><h2>Policies</h2><p>Start with job-title presets, then add record and action-level exceptions.</p><ul>{presets.map((preset) => <li key={preset}>{preset}</li>)}</ul></article><article className="card"><h2>MCP Tools</h2><p>Unauthorized tools are hidden and still blocked at invocation time.</p><ul>{tools.map((tool) => <li key={tool}><code>{tool}</code></li>)}</ul></article></section></main>; }
