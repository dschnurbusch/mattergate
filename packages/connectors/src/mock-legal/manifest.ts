import type { ConnectorManifest } from '@legal-mcp-gateway/core';
export const mockLegalManifest: ConnectorManifest = { key: 'mock-legal', displayName: 'Mock Legal', version: '0.1.0', auth: ['none'], capabilities: ['matter.read','task.write'], tools: [ { connectorKey: 'mock-legal', name: 'mock_search_matters', title: 'Search matters', description: 'Search mock matter metadata. Returns untrusted external previews.', action: 'search', resourceType: 'matter', requiredCapabilities: ['matter.read'], mutates: false, dryRunSupported: false, riskLevel: 'low', externalContent: true, inputSchema: { type: 'object', properties: { query: { type: 'string', description: 'Search text' } }, additionalProperties: false } }, { connectorKey: 'mock-legal', name: 'mock_get_matter_context', title: 'Get matter context', description: 'Fetch compact mock matter context with external-content labels.', action: 'read', resourceType: 'matter', requiredCapabilities: ['matter.read'], mutates: false, dryRunSupported: false, riskLevel: 'low', externalContent: true, inputSchema: { type: 'object', required: ['matterId'], properties: { matterId: { type: 'string' } }, additionalProperties: false } }, { connectorKey: 'mock-legal', name: 'mock_create_task', title: 'Create task', description: 'Preview or create a mock task. Real connectors should default writes to dry-run.', action: 'create', resourceType: 'task', requiredCapabilities: ['task.write'], mutates: true, dryRunSupported: true, riskLevel: 'medium', externalContent: false, inputSchema: { type: 'object', required: ['matterId','title'], properties: { matterId: { type: 'string' }, title: { type: 'string' }, dryRun: { type: 'boolean', default: true } }, additionalProperties: false } } ] };
